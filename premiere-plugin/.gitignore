node_modules/
dist/

# Vendored FFmpeg/ffprobe binaries — ~400 MB. Left over from the previous
# packaging approach and still on disk; the panel now resolves FFmpeg from
# ffmpeg-static/ffprobe-static in node_modules (see js/ffmpeg.js). Never
# committed either way.
vendor/

# Environment/brand-specific binary templates — each editor generates their own
# (see presets/README.md). Keep the folder + its README, ignore the binaries.
presets/*.sqpreset
presets/*.mogrt
